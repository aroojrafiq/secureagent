# Evaluation

The same synthetic cases will be used for both the baseline agent and the final SecureAgent workflow. This keeps the comparison consistent and avoids changing the test population between runs.

The planned primary metric is **verified vulnerability resolution rate**.

No evaluation results have been recorded yet.